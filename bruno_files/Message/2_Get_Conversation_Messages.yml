info:
  name: Get Conversation Messages
  type: http
  seq: 8

http:
  method: GET
  url: "http://localhost:4000/message/conversation/1"
  auth: inherit

headers:
  Authorization: "Bearer {{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

assertions:
  - name: Get Conversation Messages - Status is 200
    script: expect(res.getStatus()).to.equal(200);
  - name: Get Conversation Messages - Response is array
    script: expect(res.getBody()).to.be.an("array");
