info:
  name: Get All Conversations
  type: http
  seq: 4

http:
  method: GET
  url: "http://localhost:4000/conversation"
  auth: inherit

headers:
  Authorization: "Bearer {{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

assertions:
  - name: Get All Conversations - Status is 200
    script: expect(res.getStatus()).to.equal(200);
