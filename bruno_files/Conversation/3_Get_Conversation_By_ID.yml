info:
  name: Get Conversation By ID
  type: http
  seq: 5

http:
  method: GET
  url: "http://localhost:4000/conversation/1"
  auth: inherit

headers:
  Authorization: "Bearer {{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

assertions:
  - name: Get Conversation by ID - Status is 200 or 404
    script: expect([200, 404]).to.include(res.getStatus());
