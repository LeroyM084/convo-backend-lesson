info:
  name: Get Profile
  type: http
  seq: 9

http:
  method: GET
  url: "http://localhost:4000/profile/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

assertions:
  - name: Get Profile - Status is 200 or 404
    script: expect([200, 404]).to.include(res.getStatus());
