info:
  name: Get Groupe Messages
  type: http
  seq: 11

http:
  method: GET
  url: "http://localhost:4000/groupe/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

assertions:
  - name: Get Groupe Messages - Status is 200 or 404
    script: expect([200, 404]).to.include(res.getStatus());
